{
  "framework": "vite",
  "buildCommand": "pnpm install && pnpm --filter @workspace/vitrinepro run build",
  "outputDirectory": "artifacts/vitrinepro/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
